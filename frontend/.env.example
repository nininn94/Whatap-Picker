# 프론트엔드 환경변수. 실제 값은 .env.local 또는 Vercel Project Settings → Environment Variables 에 설정.
# 백엔드 API origin. Next 설정에서 브라우저 호출용 NEXT_PUBLIC_API_BASE_URL로 노출됩니다.
# 예: http://<API_SERVER_IP>:8080
API_BASE_URL=
